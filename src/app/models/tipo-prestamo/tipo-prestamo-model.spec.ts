import { TipoPrestamoModel } from './tipo-prestamo-model';

describe('TipoPrestamoModel', () => {
  it('should create an instance', () => {
    expect(new TipoPrestamoModel()).toBeTruthy();
  });
});
