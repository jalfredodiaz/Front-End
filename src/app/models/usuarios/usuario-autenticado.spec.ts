import { UsuarioAutenticado } from './usuario-autenticado';

describe('UsuarioAutenticado', () => {
  it('should create an instance', () => {
    expect(new UsuarioAutenticado()).toBeTruthy();
  });
});
