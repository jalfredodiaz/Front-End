import { CortePrestamoModel } from './corte-prestamo-model';

describe('CortePrestamoModel', () => {
  it('should create an instance', () => {
    expect(new CortePrestamoModel()).toBeTruthy();
  });
});
