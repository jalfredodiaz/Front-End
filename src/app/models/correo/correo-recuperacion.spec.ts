import { CorreoRecuperacion } from './correo-recuperacion';

describe('CorreoRecuperacion', () => {
  it('should create an instance', () => {
    expect(new CorreoRecuperacion()).toBeTruthy();
  });
});
