import { MovimientosCuentaModel } from './movimientos-cuenta-model';

describe('MovimientosCuentaModel', () => {
  it('should create an instance', () => {
    expect(new MovimientosCuentaModel()).toBeTruthy();
  });
});
